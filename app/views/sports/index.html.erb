<h1>Liste du niveau d'activite</h1>

<table>
  <tr>
    <th>Activite</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sports.each do |sport| %>
  <tr>
    <td><%= sport.activite %></td>
    <td><%= link_to 'Show', sport %></td>
    <td><%= link_to 'Back', edit_sport_path(sport) %></td>
    <td><%= link_to 'Delete', sport, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New activity', new_sport_path %>
